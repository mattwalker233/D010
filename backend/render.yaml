services:
  - type: web
    name: division-orderly-backend
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: TESSERACT_PATH
        value: /usr/bin/tesseract
      - key: PYTHON_VERSION
        value: 3.11.0 